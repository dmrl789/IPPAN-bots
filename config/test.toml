# Minimal configuration for testing

[scenario]
seed = 1
memo = "test"

[ramp]
steps = [{ tps = 100, hold_ms = 1000 }]

[target]
rpc_urls = ["http://127.0.0.1:8080"]
timeout_ms = 1000
max_in_flight = 100

[payment]
from = "test_from"
to_list_path = "keys/recipients.json"
amount_atomic = 100
fee_atomic = 2000
signing_key = "test_key"
